import { ElementStyleDirective } from './element-style.directive';

describe('ElementStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new ElementStyleDirective();
    expect(directive).toBeTruthy();
  });
});
